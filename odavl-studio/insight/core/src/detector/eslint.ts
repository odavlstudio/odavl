/**
 * ODAVL Insight - ESLint Detector (Standalone Entry)
 * Lightweight import for ESLint-specific detection
 * Bundle size: ~300 KB (vs 10 MB for full detector bundle)
 */

export { ESLintDetector } from './eslint-detector.js';
export type { ESLintError } from './eslint-detector.js';
