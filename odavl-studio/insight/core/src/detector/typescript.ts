/**
 * ODAVL Insight - TypeScript Detector (Standalone Entry)
 * Lightweight import for TypeScript-specific detection
 * Bundle size: ~500 KB (vs 10 MB for full detector bundle)
 */

export { TSDetector } from './ts-detector.js';
export type { TSError } from './ts-detector.js';
