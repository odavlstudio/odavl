# Redis Configuration for Rate Limiting

# Redis Connection
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Optional: Redis TLS (for production)
REDIS_TLS=false
REDIS_TLS_REJECT_UNAUTHORIZED=true

# Rate Limiting Configuration
# These are optional - defaults are in code
# RATE_LIMIT_FREE_TIER=60        # requests per minute
# RATE_LIMIT_PRO_TIER=300        # requests per minute
# RATE_LIMIT_ENTERPRISE_TIER=1000 # requests per minute
