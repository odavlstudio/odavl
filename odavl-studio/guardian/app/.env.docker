# ============================================
# ODAVL Guardian - Docker Environment
# ============================================
# Copy this file to .env for local Docker development:
# cp .env.docker .env

# ============================================
# Database Configuration
# ============================================
POSTGRES_DB=guardian
POSTGRES_USER=odavl
POSTGRES_PASSWORD=changeme123
POSTGRES_PORT=5432

# Full DATABASE_URL (auto-configured in docker-compose.yml)
# DATABASE_URL=postgresql://odavl:changeme123@postgres:5432/guardian?connection_limit=10&pool_timeout=5&connect_timeout=10

# ============================================
# Redis Configuration
# ============================================
REDIS_PORT=6379
REDIS_TTL=3600

# Full REDIS_URL (auto-configured in docker-compose.yml)
# REDIS_URL=redis://redis:6379

# ============================================
# Guardian App Configuration
# ============================================
GUARDIAN_PORT=3000
NODE_ENV=production

# ============================================
# Authentication (⚠️ CHANGE IN PRODUCTION!)
# ============================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=CHANGE_THIS_IN_PRODUCTION_USE_LONG_RANDOM_STRING
JWT_SECRET=CHANGE_THIS_IN_PRODUCTION_USE_LONG_RANDOM_STRING

# ============================================
# Monitoring & Error Tracking (Optional)
# ============================================
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# ============================================
# Bull Queue Configuration
# ============================================
# BULL_REDIS_URL=redis://redis:6379

# ============================================
# Performance Tuning
# ============================================
NODE_OPTIONS=--max-old-space-size=2048

# ============================================
# Security Headers (configured in middleware)
# ============================================
# Helmet.js configuration in src/lib/security/middleware.ts

# ============================================
# Generation Commands
# ============================================
# Generate secure secrets with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
