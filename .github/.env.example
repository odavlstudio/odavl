# GitHub Secrets Required for CI/CD Workflows
# ==============================================
# Copy this file to your GitHub repository settings → Secrets and variables → Actions
# Add each secret with appropriate values

# Security Scanning (Optional - workflows will skip if not set)
SNYK_TOKEN=your_snyk_token_here
# Get from: https://app.snyk.io/account

# Slack Notifications (Optional - workflows will skip if not set)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# Get from: https://api.slack.com/messaging/webhooks

# Deployment Keys (Required for deployment workflows only)
STAGING_DEPLOY_KEY=your_staging_ssh_key_here
PRODUCTION_DEPLOY_KEY=your_production_ssh_key_here

# Docker Registry (Optional - uses GITHUB_TOKEN by default)
# DOCKER_USERNAME=your_docker_username
# DOCKER_PASSWORD=your_docker_password

# NPM Publishing (Required for publish workflows only)
# NPM_TOKEN=your_npm_token_here
# Get from: https://www.npmjs.com/settings/YOUR_USERNAME/tokens

# VS Code Marketplace (Required for extension publishing only)
# VSCE_PAT=your_vscode_marketplace_token_here
# Get from: https://dev.azure.com/YOUR_ORG/_usersSettings/tokens

# Notes:
# ------
# 1. All secrets are optional for CI workflows (lint, test, build)
# 2. Deployment secrets are only needed when deploying to staging/production
# 3. Publishing secrets are only needed when releasing packages
# 4. Workflows have continue-on-error: true for optional steps
