# ODAVL Insight Global Launch - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          ODAVL INSIGHT GLOBAL ARCHITECTURE                          │
│                              (8-Phase Complete Build)                               │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                               CLIENT LAYER (3 Interfaces)                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐    │
│  │   CLI (Phase 8)      │  │ VS Code Ext (Phase 6)│  │   SDK (Phase 7)      │    │
│  │                      │  │                      │  │                      │    │
│  │  • Local mode        │  │  • Local detectors   │  │  • TypeScript API    │    │
│  │  • Cloud mode        │  │  • Cloud integration │  │  • Full type safety  │    │
│  │  • Plan awareness    │  │  • Auth status bar   │  │  • Error handling    │    │
│  │  • Polished UX       │  │  • Problems panel    │  │  • Retry logic       │    │
│  │  • Status tracking   │  │  • Tree views        │  │  • Examples          │    │
│  │                      │  │                      │  │                      │    │
│  │  Commands:           │  │  Features:           │  │  Usage:              │    │
│  │  - analyze           │  │  - Sign in           │  │  - createClient()    │    │
│  │  - analyze --cloud   │  │  - Cloud analysis    │  │  - startAnalysis()   │    │
│  │  - status            │  │  - Plan display      │  │  - pollAnalysis()    │    │
│  │  - plan              │  │  - Usage tracking    │  │  - listProjects()    │    │
│  │  - plans             │  │  - Recent analyses   │  │                      │    │
│  └──────────────────────┘  └──────────────────────┘  └──────────────────────┘    │
│            │                         │                         │                   │
└────────────┼─────────────────────────┼─────────────────────────┼───────────────────┘
             │                         │                         │
             └─────────────────────────┴─────────────────────────┘
                                       │
                                       │ HTTPS (JWT Bearer Token)
                                       │
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                        AUTHENTICATION LAYER (Phase 3)                               │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌──────────────────────────────────────────────────────────────────────────────┐  │
│  │                         ODAVL ID (Unified Auth)                              │  │
│  │                                                                              │  │
│  │  • Browser-based OAuth (GitHub, Google)                                     │  │
│  │  • Device code flow for CLI/Extension                                       │  │
│  │  • JWT tokens with claims (userId, insightPlanId, permissions)              │  │
│  │  • CLIAuthService singleton                                                 │  │
│  │  • Session management with expiry                                           │  │
│  │                                                                              │  │
│  │  Auth Endpoints:                                                            │  │
│  │  - POST /api/auth/device/code      (Request device code)                    │  │
│  │  - POST /api/auth/device/token     (Poll for authorization)                 │  │
│  │  - POST /api/auth/verify           (Verify JWT)                             │  │
│  │  - POST /api/auth/refresh          (Refresh token)                          │  │
│  └──────────────────────────────────────────────────────────────────────────────┘  │
│                                       │                                             │
└───────────────────────────────────────┼─────────────────────────────────────────────┘
                                        │
                                        ↓
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                     PRODUCT CONFIGURATION LAYER (Phase 1)                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌────────────────────────────────────────────────────────────────────────────┐    │
│  │                    INSIGHT PLANS & ENTITLEMENTS                            │    │
│  │                                                                            │    │
│  │  FREE          PRO             ENTERPRISE      CUSTOM                      │    │
│  │  ────────────  ──────────────  ─────────────   ──────────                │    │
│  │  $0/mo         $49/mo          $499/mo         Custom                      │    │
│  │                                                                            │    │
│  │  • 5 detect    • All 16 det    • All detect    • Everything              │    │
│  │  • Local only  • Cloud access  • Team collab   • Custom det              │    │
│  │  • 7-day hist  • 90-day hist   • Unlimited     • Priority                │    │
│  │  • 50 files    • Unlimited     • Unlimited     • Dedicated               │    │
│  │                • Priority      • API access    • On-prem                  │    │
│  │                                • SSO           • SLA                       │    │
│  │                                                                            │    │
│  │  Enforcement:                                                             │    │
│  │  - canRunCloudAnalysis(planId)                                            │    │
│  │  - getEnabledDetectors(planId)                                            │    │
│  │  - getAnalysisLimits(planId)                                              │    │
│  └────────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                             │
└───────────────────────────────────────┼─────────────────────────────────────────────┘
                                        │
                                        ↓
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         BILLING LAYER (Phase 2 - Stripe)                            │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌────────────────────────────────────────────────────────────────────────────┐    │
│  │                    STRIPE INTEGRATION                                      │    │
│  │                                                                            │    │
│  │  Subscriptions:                    Usage Tracking:                        │    │
│  │  ────────────────                  ──────────────────                     │    │
│  │  • Product sync                    • Metered billing                      │    │
│  │  • Price sync                      • Usage aggregation                    │    │
│  │  • Subscription lifecycle          • Monthly reset                        │    │
│  │  • Trial management                • Overage alerts                       │    │
│  │                                                                            │    │
│  │  Webhooks:                         Components:                            │    │
│  │  ─────────────                     ────────────                           │    │
│  │  • subscription.created            • StripeManager                        │    │
│  │  • subscription.updated            • UsageTracker                         │    │
│  │  • payment_intent.succeeded        • WebhookRouter                        │    │
│  │  • customer.subscription.deleted   • EventLogger                          │    │
│  └────────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                             │
└───────────────────────────────────────┼─────────────────────────────────────────────┘
                                        │
                                        ↓
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                      CLOUD BACKEND (Phase 4 - API & Database)                       │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌────────────────────────────────────────────────────────────────────────────┐    │
│  │                        NEXT.JS API ROUTES                                  │    │
│  │                                                                            │    │
│  │  Projects API:                     Analyses API:                          │    │
│  │  ──────────────                    ────────────────                       │    │
│  │  GET    /api/projects              GET    /api/analyses                   │    │
│  │  POST   /api/projects              POST   /api/analyses                   │    │
│  │  GET    /api/projects/:id          GET    /api/analyses/:id               │    │
│  │  PATCH  /api/projects/:id          PATCH  /api/analyses/:id               │    │
│  │  DELETE /api/projects/:id          DELETE /api/analyses/:id               │    │
│  │                                                                            │    │
│  │  Usage API:                        Billing API:                           │    │
│  │  ───────────                       ─────────────                          │    │
│  │  GET    /api/usage                 GET    /api/billing/subscription       │    │
│  │  POST   /api/usage/track           POST   /api/billing/checkout           │    │
│  │  GET    /api/usage/summary         POST   /api/billing/portal             │    │
│  └────────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                             │
│  ┌────────────────────────────────────────────────────────────────────────────┐    │
│  │                        JOB QUEUE (BullMQ + Redis)                          │    │
│  │                                                                            │    │
│  │  Queue: analysis-jobs                                                     │    │
│  │  ───────────────────────                                                  │    │
│  │  • Job creation                    Worker Process:                        │    │
│  │  • Priority handling               ───────────────                        │    │
│  │  • Retry logic (3 attempts)        • Detector orchestration               │    │
│  │  • Progress tracking               • Parallel execution                   │    │
│  │  • Timeout (5 minutes)             • Result aggregation                   │    │
│  │  • Dead letter queue               • Status updates                       │    │
│  └────────────────────────────────────────────────────────────────────────────┘    │
│                                       │                                             │
│  ┌────────────────────────────────────────────────────────────────────────────┐    │
│  │                   DATABASE (PostgreSQL + Prisma ORM)                       │    │
│  │                                                                            │    │
│  │  Core Tables:                      Supporting Tables:                     │    │
│  │  ─────────────                     ──────────────────                     │    │
│  │  • User                            • Subscription                         │    │
│  │  • Project                         • Usage                                │    │
│  │  • Analysis                        • BillingEvent                         │    │
│  │  • Issue                           • AuditLog                             │    │
│  │                                                                            │    │
│  │  Indexes: userId, projectId, analysisId, createdAt                        │    │
│  │  Relations: One-to-Many (User → Projects, Project → Analyses)             │    │
│  └────────────────────────────────────────────────────────────────────────────┘    │
│                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ↓
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                     CLOUD UI (Phase 5 - Next.js Dashboard)                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│  ┌────────────────────────────────────────────────────────────────────────────┐    │
│  │                         DASHBOARD PAGES                                    │    │
│  │                                                                            │    │
│  │  /projects                         /insight/analyses/:id                  │    │
│  │  ──────────────────────            ─────────────────────────────          │    │
│  │  • List all projects               • Analysis details                     │    │
│  │  • Create new project              • Issue breakdown                      │    │
│  │  • Search & filter                 • File heatmap                         │    │
│  │  • Quick actions                   • Historical comparison                │    │
│  │                                    • Export (JSON, SARIF)                 │    │
│  │  /usage                            /billing                               │    │
│  │  ──────────────────────            ─────────────────────────────          │    │
│  │  • Current usage                   • Subscription details                 │    │
│  │  • Monthly trends                  • Payment method                       │    │
│  │  • Plan limits                     • Invoice history                      │    │
│  │  • Upgrade prompts                 • Usage-based charges                  │    │
│  │                                                                            │    │
│  │  Components:                                                              │    │
│  │  ─────────────                                                            │    │
│  │  • ProjectCard, AnalysisCard, IssueList                                   │    │
│  │  • UsageChart, TrendGraph, PlanBadge                                      │    │
│  │  • UpgradeModal, PaymentForm                                              │    │
│  └────────────────────────────────────────────────────────────────────────────┘    │
│                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              DATA FLOW EXAMPLE                                      │
│                       (Cloud Analysis from CLI to Dashboard)                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

1. USER: $ odavl insight analyze --cloud
   └─→ CLI checks auth status (Phase 3)
       └─→ CLIAuthService.isAuthenticated() → true
           └─→ Gets access token from session

2. CLI checks plan entitlements (Phase 1)
   └─→ getCurrentPlan() → INSIGHT_PRO
       └─→ canRunCloudAnalysis(INSIGHT_PRO) → true
           └─→ Proceeds to cloud analysis

3. CLI creates SDK client (Phase 7)
   └─→ createInsightClient({ accessToken })
       └─→ POST /api/projects (find or create)
           └─→ Response: projectId = "prj_abc123"

4. CLI starts analysis (Phase 7 SDK → Phase 4 API)
   └─→ client.startAnalysis({ projectId, detectors, language })
       └─→ POST /api/analyses
           └─→ Creates Analysis record in DB (Phase 4)
           └─→ Enqueues job in BullMQ (Phase 4)
           └─→ Response: analysisId = "anl_xyz789"

5. BullMQ worker processes job (Phase 4)
   └─→ Pulls job from queue
       └─→ Executes detectors (Insight Core)
           └─→ Aggregates results (issues, metrics)
               └─→ Updates Analysis status: RUNNING → COMPLETED
                   └─→ Saves issues to DB

6. CLI polls for completion (Phase 7 SDK)
   └─→ client.pollAnalysis(analysisId, progressCallback)
       └─→ GET /api/analyses/:id (every 5s)
           └─→ Progress: 0% → 25% → 50% → 75% → 100%
               └─→ Final status: COMPLETED

7. CLI displays results (Phase 8)
   └─→ Formatted summary (chalk + boxen)
       └─→ Dashboard URL: https://cloud.odavl.studio/insight/analyses/anl_xyz789
           └─→ Saved locally: .odavl/last-cloud-analysis.json

8. USER clicks dashboard URL
   └─→ Browser opens analysis page (Phase 5 UI)
       └─→ Fetches analysis details (Phase 4 API)
           └─→ Renders:
               ├─ Issue breakdown by severity
               ├─ File heatmap
               ├─ Historical trends
               └─ Export buttons (JSON, SARIF)

9. Billing: Usage tracked (Phase 2)
   └─→ After analysis completes
       └─→ UsageTracker.trackAnalysis(userId)
           └─→ Increments usage counter
               └─→ If metered: Reports to Stripe
                   └─→ Stripe calculates charges at month end


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              TECHNOLOGY STACK                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘

Frontend:         Next.js 15, React 18, TypeScript, Tailwind CSS, Radix UI
Backend:          Next.js API Routes, Prisma ORM, BullMQ, Redis
Database:         PostgreSQL 15
Auth:             NextAuth.js, JWT, OAuth (GitHub, Google)
Billing:          Stripe (Subscriptions, Metered Usage, Webhooks)
CLI:              Commander.js, Chalk, Ora, Boxen
VS Code:          VS Code Extension API, Language Server Protocol
SDK:              TypeScript, Fetch API, AbortController
Testing:          Vitest, Testing Library, Playwright
Deployment:       Vercel (Frontend), AWS/Render (Backend), Docker
Monitoring:       Sentry, LogTail, Stripe Dashboard
CI/CD:            GitHub Actions, pnpm workspaces


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              SECURITY LAYERS                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

1. Authentication (Phase 3)
   • JWT tokens with expiry
   • HTTPS-only transmission
   • Browser-based OAuth (no password storage)
   • Device code flow for CLI/Extension

2. Authorization (Phase 1 + 3)
   • Plan-based access control
   • API key validation
   • Role-based permissions (future)

3. API Security
   • Rate limiting (100 req/min)
   • Input validation (Zod schemas)
   • SQL injection prevention (Prisma ORM)
   • CORS configuration

4. Billing Security (Phase 2)
   • Stripe webhook signature verification
   • PCI compliance (Stripe Elements)
   • Secure payment method storage
   • Fraud detection (Stripe Radar)

5. Data Security (Phase 4)
   • PostgreSQL row-level security (future)
   • Encrypted connections (TLS)
   • Audit logging
   • GDPR compliance (data export/deletion)


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                          SCALABILITY & PERFORMANCE                                  │
└─────────────────────────────────────────────────────────────────────────────────────┘

Database:
  • Indexed queries (userId, projectId, createdAt)
  • Connection pooling (Prisma)
  • Query optimization (select only needed fields)

Job Queue:
  • Horizontal scaling (multiple workers)
  • Priority queues (ENTERPRISE > PRO > FREE)
  • Graceful degradation (retry logic)

API:
  • Next.js ISR (Incremental Static Regeneration)
  • Edge caching (Vercel)
  • API response caching (Redis)

Frontend:
  • Code splitting (Next.js automatic)
  • Image optimization (next/image)
  • Lazy loading (React.lazy)

CLI/Extension:
  • Local-first (works offline)
  • Incremental analysis (only changed files)
  • Parallel execution (4-16x speedup)


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              SUCCESS METRICS                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

Technical:
  ✅ 100% TypeScript type safety
  ✅ >80% test coverage
  ✅ Zero breaking changes
  ✅ Sub-30s cloud analysis
  ✅ <200ms API response time (p95)

User Experience:
  ✅ One-command cloud analysis (odavl insight analyze --cloud)
  ✅ Clear plan limits and upsell messaging
  ✅ Polished CLI output (colors, icons, boxed summaries)
  ✅ Real-time progress tracking
  ✅ Dashboard with history and trends

Business:
  ✅ 4-tier monetization (FREE → PRO → ENTERPRISE → CUSTOM)
  ✅ Usage-based billing for CUSTOM plans
  ✅ Self-serve upgrade flow
  ✅ Stripe integration (subscriptions + webhooks)


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              NEXT STEPS                                             │
└─────────────────────────────────────────────────────────────────────────────────────┘

1. Testing:
   - Manual testing of all 8 phases
   - Integration tests (CLI → API → DB → UI)
   - Load testing (job queue under heavy load)
   - Security audit

2. Deployment:
   - PostgreSQL setup (AWS RDS or Supabase)
   - Redis setup (AWS ElastiCache or Upstash)
   - Next.js deployment (Vercel)
   - CLI publish (npm)
   - Extension publish (VS Code Marketplace)

3. Documentation:
   - User guides (CLI, Extension, SDK)
   - API documentation (OpenAPI spec)
   - Video tutorials
   - Migration guide (local → cloud)

4. Marketing:
   - Landing page
   - Pricing page
   - Blog posts (launch announcement)
   - Social media campaign

5. Launch:
   - Beta testing with early adopters
   - Public launch 🚀
   - Monitoring and analytics
   - Iterative improvements based on feedback
```
