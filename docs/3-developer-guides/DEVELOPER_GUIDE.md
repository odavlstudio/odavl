# 👨‍💻 Developer Guide

This guide explains workspace layout, scripts, and conventions.

- Run website: `pnpm dev --filter odavl-website`
- Run extension: `pnpm build --filter vscode-ext`
- Run tests: `pnpm test`
- Lint & typecheck: `pnpm lint` / `pnpm typecheck`
- Policy limits: ≤ 40 LOC / ≤ 10 files per commit
- Shadow CI runs on every push to main
