# ODAVL API Reference

## Available Endpoints (planned)

- GET /api/v1/health — Returns system status
- GET /api/v1/policies — Lists active policies
- POST /api/v1/report — Submits verification results

## SDK Integration

Use `@odavl/sdk` (coming soon) for programmatic access.
